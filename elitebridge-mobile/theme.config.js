/** @type {const} */
const themeColors = {
  // Elite Bridge Staffing branding - Dark forest green with gold accents
  primary: { light: '#1B5E3F', dark: '#2D8659' },      // Forest green (light/dark variants)
  background: { light: '#FFFFFF', dark: '#0F172A' },   // White to dark slate
  surface: { light: '#F8FAFC', dark: '#1E293B' },      // Light slate to dark slate
  foreground: { light: '#0F172A', dark: '#F1F5F9' },   // Dark slate to light slate
  muted: { light: '#64748B', dark: '#94A3B8' },        // Slate gray
  border: { light: '#E2E8F0', dark: '#334155' },       // Light to dark border
  success: { light: '#16A34A', dark: '#4ADE80' },      // Green
  warning: { light: '#D97706', dark: '#FCD34D' },      // Gold/Orange (Elite Bridge accent)
  error: { light: '#DC2626', dark: '#EF4444' },        // Red
};

module.exports = { themeColors };
